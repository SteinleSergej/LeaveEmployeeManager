@page
@using LeaveEmployeeManager.Models
@model LeaveEmployeeManager.Pages.LeaveTypes.IndexModel
@{
}
<div>
    <h1><span class="text-secondary">L</span>eave <span class="text-secondary">T</span>ypes <i class="bi bi-signpost-split text-secondary"></i></h1>
</div>
<div class="container">
    <a asp-page="Create" class="btn btn-secondary shadow">Create</a>
    @foreach (var leave in Model?.LeaveTypes ?? Enumerable.Empty<LeaveType>())
    {
      
        
        
 
        <partial name="_DisplayLeaveType" model="leave"/>
    }
    @if(Model?.LeaveTypes?.Count() == 0)
    {
        <div class="row"> 
            <div class=" col-12 " style="height:50vh;">
                <div class="h-100 d-flex justify-content-center align-items-center">
                    <i class="bi bi-wrench-adjustable-circle text-dark" style="font-size:20rem;opacity:30%;"></i>
                    </div>
            </div>
        </div>
    }
</div>